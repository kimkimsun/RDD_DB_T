using UnityEngine;

public class NPC_Quest_Mgr : MonoBehaviour
{
    public enum QuestType
    {
        type1,
        type2, 
        type3, 
        type4, 
        type5, 
        type6, 
        type7,
    }

    public QuestType quest_type;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void finishFunction(string condition)
    {

    }

    public void finishFunction(int condition)
    {

    }

    public void finishFunction(float condition)
    {

    }

    public void finishFunction(bool condition)
    {

    }
}
